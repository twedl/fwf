[package]
name = "fwf"
version = "0.1.0"
edition = "2024"
description = "Read fixed-width files into Arrow record batches"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
